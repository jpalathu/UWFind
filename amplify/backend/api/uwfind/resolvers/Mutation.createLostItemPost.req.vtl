#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createLostItemPostInput.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createLostItemPostInput[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO LostItemPost $colStr VALUES $valStr", "SELECT * FROM LostItemPost WHERE postID=$ctx.args.createLostItemPostInput.postID"]
}