#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createImageInput.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createImageInput[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO Image $colStr VALUES $valStr", "SELECT * FROM Image WHERE imageID=$ctx.args.createImageInput.imageID"]
}